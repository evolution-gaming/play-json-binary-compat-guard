name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  release:
    uses: evolution-gaming/scala-github-actions/.github/workflows/release.yml@v6
    with:
      verify_sbt_command: '+clean; +check; +all testFull package'
    secrets: inherit
